type Review {
    id: ID!
    rating: Int!
    comment: String
    training: Training!
    member: Member!
}

input ReviewInput {
    rating: Int
    comment: String
    trainingId: Int
    memberId: Int
}
